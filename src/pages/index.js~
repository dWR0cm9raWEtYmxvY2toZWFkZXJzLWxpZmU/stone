import React from 'react'
import Link from 'gatsby-link'
import '../assets/styles/page.css'
import cn from '../assets/text/cn'

const IndexPage = () => (
    <div>
        <section className="ctn banner">
            <h1>Hi people</h1>
        </section>
        <section className="ctn bg1">
            <h4>{cn.intro.title}</h4>
            <p className="para">{cn.intro.para1}</p>
            <p className="para">{cn.intro.para2}</p>
        </section>
        <section className="ctn">
            <h4>{cn.lookback.title}</h4>
            <p className="para">{cn.lookback.para}</p>
        </section>
        <section className="ctn bg1">
            <h4>{cn.overview.title}</h4>
            <p className="para">{cn.overview.para}</p>
        </section>
        <section className="ctn">
            <h4>{cn.star.title}</h4>
            <p className="para">{cn.star.para}</p>
        </section>
        <section className="ctn bg1">
            <h4>{cn.star.title}</h4>
            <p className="para">{cn.overview.para}</p>
        </section>
        <section className="ctn">
            <h4>{cn.host.title}</h4>
            <p className="para">{cn.host.blockGeek}</p>
            <p className="para">{cn.host.gdex}</p>
        </section>
        <section className="ctn bg1">
            <h4>{cn.assist.title}</h4>
            <p className="para">{cn.assist.para}</p>
        </section>
        <section className="ctn">
            <h4>{cn.partner.title}</h4>
        </section>
    </div>
)

export default IndexPage
